/*import type { BlockModel } from '@algogrind/store';

import type { RootBlockComponent } from '../../index.js';

export interface EmojiMenuCategory {
  name: string;
  icon: string;
  emojis: EmojiMenuActionItem[];
}

export interface EmojiMenuActionItem {
  char: string;
  shortcodes: string[];
}

export interface EmojiMenuContext {
  rootComponent: RootBlockComponent;
  model: BlockModel;
}

export interface EmojiMenuConfig {
  ignoreBlockTypes: BlockSuite.Flavour[];
  maxHeight: number;
  items: EmojiMenuCategory[];
}

export type EmojiMenuStaticConfig = Omit<EmojiMenuConfig, 'items'> & {
  items: EmojiMenuCategory[];
};

export const defaultEmojiMenuConfig: EmojiMenuConfig = {
  ignoreBlockTypes: ['affine:code'],
  maxHeight: 344,
  items: [
    {
      name: 'People',
      icon: '😀',
      emojis: [
        { char: '😀', shortcodes: ['grinning'] },
        { char: '😁', shortcodes: ['grin'] },
        { char: '😂', shortcodes: ['joy'] },
        { char: '🤣', shortcodes: ['rofl'] },
        { char: '😃', shortcodes: ['smiley'] },
        { char: '😄', shortcodes: ['smile'] },
        { char: '😅', shortcodes: ['sweat_smile'] },
        { char: '😆', shortcodes: ['laughing'] },
        // ... extend for all modern phone emojis
      ],
    },
    {
      name: 'Animals & Nature',
      icon: '🐶',
      emojis: [
        { char: '🐶', shortcodes: ['dog'] },
        { char: '🐱', shortcodes: ['cat'] },
        { char: '🐭', shortcodes: ['mouse'] },
        { char: '🐹', shortcodes: ['hamster'] },
        // ... extend
      ],
    },
    {
      name: 'Food & Drink',
      icon: '🍏',
      emojis: [
        { char: '🍏', shortcodes: ['green_apple'] },
        { char: '🍎', shortcodes: ['apple'] },
        { char: '🍐', shortcodes: ['pear'] },
        // ... extend
      ],
    },
    // ... more categories
  ],
};
*/
console.log('.');
